apiVersion: apps/v1
kind: Deployment
metadata:
  name: liquibase-pod-test
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: liquibase
  template:
    metadata:
      labels:
        app: liquibase
    spec:
      containers:
      - name: aws-cli
        image: 039765606255.dkr.ecr.us-east-1.amazonaws.com/aws-cli:latest
        command: ["sleep", "1000"]

